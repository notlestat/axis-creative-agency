# Persistent client learning

Use templates/reviews/learning-note.md when collecting results. Accept client feedback, rejected/approved ideas, preferred/banned styles, hook performance, winning/weak creative, platform results, CTR, CPA, ROAS, watch time and hold rate. Record period, platform, objective, attribution window, source and denominators. Distinguish observed metric from interpretation and future hypothesis.

Append with `learn <client> <campaign> --file <note>`. The tool preserves existing history and suppresses identical reimports for the same campaign. Correct old conclusions through a new dated note, never rewrite the original. Update client creative preferences only from explicit feedback, linking the dated learning note.

Future strategy reads this file before planning. New client knowledge can stale earlier creative approvals because their inputs changed; old approval events remain in history. This is expected, not deletion of earlier decisions.
