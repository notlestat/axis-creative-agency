# Axis handoff

Require current final asset approval and exactly one selected PASS asset per approved shot. Run `prepare <client> <campaign> handoff`. Review 10_approved/post-production-handoff.md for approved shot order, actual filenames/hashes, durations, edit intent, transitions, typography, copy, captions, music, sound, colour, graphics, aspect ratios and platform versions.

The document includes the approved source direction so the editor receives context. Verify its file paths resolve relative to the campaign. Call out any source-specific usage limits from references. Deliver the document. Do not invoke, configure or modify the separate Axis post-production workflow unless the user explicitly asks.
